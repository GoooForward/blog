# XXXX子系统（例如pinctrl）

## 0x00 背景

0.前置知识（可选，如果有的话）
1.该子系统是什么
2.这个子系统的作用作用是什么
3.这个子系统的架构是怎么样的
4.大体架构图

## 0x01 主要结构体

这部分填写主要结构体，并且要对结构体内的成员变量做中文注释
（结束后总结下这些结构体直接的关系，最好用图描述下）

## 0x02 主要API

这部分填写主要的API，并且要有这个API函数的代码实现（不要devm版本）
并且如果函数内有函数调用，要递归追踪所有的调用逻辑（并且要有中文注释）

## 0x03 初始化逻辑

这部分填写这个子系统是如何实现的初始化，先展示所有的调用逻辑栈，然后展示具体的代码调用（并且要有中文注释）

## 0x04 示例

给出一个使用此子系统的示例

